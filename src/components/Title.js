import { Col, Row } from "react-bootstrap";

const Title = () => {
  return (
    <Row className="text-center py-5">
      <Col>
        <h2>My Movie List</h2>
      </Col>
    </Row>
  );
};

export default Title;
